# Legacy public PNG exports

These raster product images were previously stored under `public/product-images/`.
They are retained here as source/reference material but are intentionally outside
Vite's `public` directory so they are not copied into production builds.

The active product catalog uses the corresponding SVG assets under
`public/product-images/`.
